#include <stdio.h>
#include <windows.h>
#include <locale.h>
#include "entrega.h"

#define valProt 7.5
#define entAdic 4

int main(){
    setlocale(LC_ALL, ".UTF8");
    SetConsoleOutputCP(CP_UTF8);

    //Variáveis e const
    float distKM, peso, subTot, total;
    int mod, opProt, opc;
    //============================


    return 0;
}